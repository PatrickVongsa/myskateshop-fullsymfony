{% extends 'base.html.twig' %}

{% block title %}
	{{parent()}}
	- Créer nouvelle catégorie
{% endblock %}

{% block body %}
	<div class="container content-container">
		<h1 class="text-primary mt-5 mb-2">Créer nouvelle catégorie</h1>
		<div class="row mb-2">
			<div class="col text-end">
				<a class="btn btn-secondary" href="{{ path('category_index') }}">Retour à la liste</a>
			</div>
		</div>

		<div class="row mb-5 justify-content-center">
			<div class="col-12 col-md-8">
				{{ include('category/_form.html.twig') }}
			</div>
		</div>
	</div>
{% endblock %}
