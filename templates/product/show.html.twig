{% extends 'base.html.twig' %}

{% block title %}
	{{parent()}}
	- Détails
	{{product.name}}
{% endblock %}

{% block body %}
	<div class="container content-container">
		<h1 class="text-primary my-5">Détails
			{{product.name}}</h1>
		<div class="row">
			<div class="col d-flex justify-content-end">
				<a class="btn btn-secondary me-2" href="{{ path('product_index') }}">Retour à la liste</a>
				<a class="btn btn-warning me-2" href="{{ path('product_edit', {'id': product.id}) }}">Modifier</a>
				{{ include('product/_delete_form.html.twig') }}
			</div>
		</div>
    <div class="row justify-content-center my-5">
    <div class="col-12 col-md-6 col-lg-3 mt-3 mb-3 mb-md-0">
        {% include "components/_card-product-show.html.twig" %}
    </div>
    <div class="col-12 col-md-6 col-lg-8">
		<table class="table">
			<tbody>
				<tr>
					<th>Id</th>
					<td>{{ product.id }}</td>
				</tr>
				<tr>
					<th>Nom</th>
					<td>{{ product.name }}</td>
				</tr>
				<tr>
					<th>Description</th>
					<td>{{ product.description }}</td>
				</tr>
				<tr>
					<th>Prix</th>
					<td>{{ product.price }}</td>
				</tr>
				<tr>
					<th>Taille</th>
					<td>{{ product.size }}</td>
				</tr>
				<tr>
					<th>Stock</th>
					<td>{{ product.stock }}</td>
				</tr>
				<tr>
					<th>Stock minimum</th>
					<td>{{ product.minStock }}</td>
				</tr>
				<tr>
					<th>Est en ligne ?</th>
					<td>{{ product.isVisible ? 'Oui' : 'Non' }}</td>
				</tr>
			</tbody>
		</table>
    </div>
    </div>
	</div>
{% endblock %}
